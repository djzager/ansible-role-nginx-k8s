---

apiVersion: v1
kind: Service
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
  labels:
    app: {{ name }}
spec:
  ports:
  - name: web
    port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: {{ name }}
